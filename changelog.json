[
  {
    "version": "v2.0.7",
    "date": "29/07/2026",
    "title": "Headless Auth Now Actually Applies & Inline Block Help",
    "changes": {
      "Added": [
        "The Auth block now shows an inline \"?\" help tooltip in its header, matching the other request blocks"
      ],
      "Fixed": [
        "The Auth block (Bearer, Basic, API Key) is now parsed and actually applied as request headers/query params when running headlessly via the CLI or voiden-mcp — previously nothing in the headless request-build pipeline read the auth block at all, so every CLI/MCP request silently ran with no authentication regardless of what was configured in the Auth tab, while the real Voiden app's Send button (which applies auth in the renderer before the request ever leaves it) was never affected",
        "Merges its auth-derived headers/query params with whatever another plugin's request-build handler already contributed (e.g. voiden-rest-api's headers-table rows) instead of overwriting them, regardless of which plugin's handler runs first"
      ]
    }
  },
  {
    "version": "v2.0.6",
    "date": "17/07/2026",
    "title": "Block Documentation Link & Auth Table Cell Borders",
    "changes": {
      "Added": [
        "An \"Open Documentation\" item now appears in the Auth block's right-click block menu, opening the Voiden Blocks docs in your system browser"
      ],
      "Fixed": [
        "Auth block now shows the same bordered card and cell grid as other request tables (Headers, Query Params, etc.) — every auth type's fields (Basic, Bearer, API Key, OAuth 2.0, and the rest) were previously borderless, making cells hard to tell apart, especially in low-contrast themes"
      ]
    }
  },
  {
    "version": "v2.0.5",
    "date": "03/07/2026",
    "title": "Copy/Paste as cURL Auth Fixes & Skill Documentation Expansion",
    "changes": {
      "Added": [
        "Pasting a cURL command now builds the matching auth block: Basic, Digest, NTLM, AWS Signature, and Bearer are fully reconstructed; OAuth 1.0 is fully reconstructed when signed PLAINTEXT (consumer/token secrets are recoverable from the signature) or partially reconstructed with a warning for HMAC-signed requests (secrets aren't recoverable from a one-way signature); Hawk recovers the id with a warning that the key must be re-entered; Netrc shows a warning instead of a block, since machine/login/password live in ~/.netrc rather than the pasted command"
      ],
      "Fixed": [
        "Copy as cURL now includes Digest, NTLM, AWS Signature, Netrc, and OAuth 1.0 (PLAINTEXT) auth — a fragile dynamic import used to reach the cURL header extender registry and was silently dropping every one of these auth types from the generated command with no error surfaced anywhere; registration and lookup now go through a direct context reference instead",
        "Copy as cURL no longer repeats the same auth flags multiple times in the generated command",
        "Netrc auth in Copy as cURL now includes the actual login/password via -u instead of a bare --netrc flag, which only works if the machine running the command happens to already have a matching ~/.netrc entry",
        "OAuth 2.0 auth in Copy as cURL now embeds the actual resolved access token instead of an unresolved {{process.*}} placeholder, which has no meaning outside Voiden",
        "Copy as cURL now surfaces a warning toast for Hawk and Atlassian ASAP auth, which require a live-computed signature/JWT with no static cURL representation, instead of silently omitting them"
      ],
      "Improved": [
        "AI skill documentation significantly expanded with full OAuth2 configuration reference (all grant types, PKCE, token fields), complete auth block attr schemas, and Atlassian ASAP auth method documentation"
      ]
    }
  },
  {
    "version": "v2.0.4",
    "date": "01/07/2026",
    "title": "cURL Header Extender & Build Fix",
    "changes": {
      "Added": [
        "Plugin API for extending cURL output — plugins can register a `registerCurlHeaderExtender` function to inject headers, query params, or raw cURL flags into the cURL command produced by CopyCurlButton"
      ],
      "Fixed": [
        "Remove catch-all shim from build pipeline for correct dependency bundling"
      ]
    }
  },
  {
    "version": "v2.0.2",
    "date": "27/05/2026",
    "title": "OAuth2 Error Toasts & Shim Fix",
    "changes": {
      "Added": [
        "Block outline metadata for the auth block so it displays at its correct document position in the Block Overview Panel, with the auth type (Bearer Token, OAuth 2.0, etc.) shown as the preview"
      ],
      "Fixed": [
        "OAuth2 auto-acquire and auto-refresh now show toast with specific error when config fields are missing or token server returns error",
        "Removed catch-all shim block"
      ]
    }
  },
  {
    "version": "v2.0.0",
    "date": "10/05/2026",
    "title": "SDK v2 Migration",
    "description": "Rewritten on the new SDK pipeline architecture with improved OAuth2 support.",
    "changes": {
      "Added": [
        "OAuth2 Client Credentials, Authorization Code, and Password flows",
        "Automatic token refresh before expiry",
        "AWS Signature v4 authentication",
        "Digest authentication",
        "NTLM authentication support"
      ],
      "Improved": [
        "Bearer token block redesigned with token preview",
        "Basic auth now encodes correctly for all character sets"
      ]
    }
  }
]
